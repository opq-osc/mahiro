import { Callout } from 'nextra-theme-docs'

# API

## Group

### `mahiro.ctx`

消息上下文

```python
  # OPQ 消息体
  mahiro.ctx.msg
  # 消息来源
  mahiro.ctx.{userId, userNickname, groupId, groupNickname}
  # Bot 账号
  mahiro.ctx.qq
```

### `mahiro.sender`

发送消息方法

```python
  # 文本消息
  mahiro.sender.send_to_group(group_id, msg="text")

  # 图片消息
  mahiro.sender.send_to_group(
    group_id, 
    # 支持路径，url，base 64
    fast_image="/User/path/to/image.png",
    fast_image="http://domain.com/image.jpg",
    fast_image="data:image/jpeg;base64,/9....,
  )
```

## Friend

### `mahiro.ctx`

消息上下文

```python
  # OPQ 消息体
  mahiro.ctx.msg
  # 消息来源
  mahiro.ctx.{userId, userName}
  # Bot 账号
  mahiro.ctx.qq
```

### `mahiro.sender`

发送消息方法

```python
  # 文本消息
  mahiro.sender.send_to_friend(user_id, msg="text")

  # 图片消息
  mahiro.sender.send_to_friend(
    user_id, 
    # 支持路径，url，base 64
    fast_image="/User/path/to/image.png",
    fast_image="http://domain.com/image.jpg",
    fast_image="data:image/jpeg;base64,/9....,
  )
```

## 其他

### 连续对话

目前没有实现直接的 API ，但你可以自行存储对话的阶段数据，来自己实现连续对话。

<Callout type="warning" emoji="⚠️">
  <div style={{ fontSize: 14 }}>由于 Python Bridge 是一种转发方式，从而兼容一些 Python 插件的运作。<br /> 在未来，我们更推荐直接编写 JavaScript 代码，以便获得更好的性能，完美与 Mahiro 洽和，接入更丰富的功能。 </div>
</Callout>
