# 更新日志

### 6.0.0

 - Required Bridge: `v1.5.0`

 - Python Bridge 可以通过 `raw` 字段获得原生 OPQ 消息内容

### 5.0.1

 - 改善了一些手机端面板适配样式

### 5.0.0

 - 优化了与 Python Bridge 的链接，支持任一方的重启后重加载

 - 优化了 账号管理 扫码登录

### 3.0.1

 - 在 Python Bridge 响应非 `200` 时不抛出错误

### 3.0.0

 - 默认打开 `MAHIRO_AUTH_TOKEN` 权限校验，保护面板安全

 - 支持多账号

### 2.0.0

 - 新增 `sqlite` 国内源加速说明

 - 修复 `@vscode/sqlite3` 在 pnpm 下 `tar` 隐形依赖的问题

### 1.0.1

 - 第一次发布版本


